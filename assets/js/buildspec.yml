version: 0.1

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - echo nothing to do yet
artifacts:
  files:
    - index.html
    - contact__left-map.hmtl
    - assets/*